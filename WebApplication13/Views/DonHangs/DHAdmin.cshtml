@model IEnumerable< IGrouping<Int32,WebApplication13.Models.DonHang>>

@{
    ViewBag.Title = "DHAdmin";
    Layout = "~/Views/Layout/_LayoutAdminNew.cshtml";
}

<h2>DHAdmin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
        </th>
        <th>
           
        </th>
        <th>
            
        </th>
        <th>
        </th>
        <th>
        </th>
        <th>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KhachHang.TenKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuongBan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HinhThucTT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TongTien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayMua)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonHangId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonHangId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonHangId })
        </td>
    </tr>
}

</table>
